<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <div id="linklist">
        <% for(let i in itemlist) { %>
            <ul>
                <li><%= itemlist[i].name %></li>
            </ul>
        <% } %>
    </div>
</body>
</html>